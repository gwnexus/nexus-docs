export const metadata = {
  title: 'REST API',
  description: 'The Nexus REST API — endpoints for projects, sessions, ADRs, tasks, dispatches, and knowledge base operations.',
}
export const sections = []

# REST API

<Note>
  This page is under construction. Full API reference coming soon.
</Note>

The Nexus REST API is the foundation that `nexus-mcp` is built on. All MCP tools are thin wrappers over the REST API.

Base URL: `https://nexus.gatewarden.eu/api`

Authentication: Bearer token (project-scoped API token from the dashboard).
